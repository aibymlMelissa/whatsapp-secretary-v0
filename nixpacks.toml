[phases.setup]
nixPkgs = ["python310", "nodejs-18_x", "chromium"]

[phases.install]
cmds = [
  "cd backend && python3 -m pip install -r requirements.txt",
  "cd backend/whatsapp_client && npm install"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend && python3 app.py"
