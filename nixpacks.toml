[phases.setup]
nixPkgs = ["python39", "nodejs-18_x"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt", "cd backend/whatsapp_client && npm install"]

[start]
cmd = "cd backend && uvicorn app:app --host 0.0.0.0 --port $PORT"
